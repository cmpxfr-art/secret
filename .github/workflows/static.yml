<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>For You😳</title>

  <style>
    body {
      margin: 0;
      height: 100vh;
      background: linear-gradient(135deg, #ffdde1, #ee9ca7);
      font-family: 'Segoe UI', sans-serif;
      display: flex;
      align-items: center;
      justify-content: center;
      overflow: hidden;
    }

    .screen {
      text-align: center;
    }

    .hidden {
      display: none;
    }

    /* Flower */
    .flower {
      font-size: 80px;
      cursor: pointer;
      animation: bounce 1.5s infinite;
    }

    @keyframes bounce {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-15px); }
    }

    /* Envelope */
    .envelope {
      font-size: 90px;
      cursor: pointer;
      animation: wiggle 1.2s infinite;
    }

    @keyframes wiggle {
      0%, 100% { transform: rotate(0); }
      25% { transform: rotate(-5deg); }
      75% { transform: rotate(5deg); }
    }

    /* Card */
    .card {
      background: white;
      padding: 40px;
      border-radius: 20px;
      max-width: 420px;
      box-shadow: 0 20px 40px rgba(0,0,0,0.25);
    }

    h1 {
      color: #ff4b6e;
    }

    p {
      color: #555;
      font-size: 1.1em;
      line-height: 1.6;
    }

    .buttons {
      margin-top: 25px;
    }

    button {
      padding: 12px 25px;
      font-size: 1em;
      border: none;
      border-radius: 25px;
      cursor: pointer;
      margin: 5px;
    }

    #yesBtn {
      background: #ff4b6e;
      color: white;
    }

    #noBtn {
      background: #ccc;
      color: #333;
      position: absolute;
    }
  </style>
</head>

<body>

  <!-- Screen 1: Flower -->
  <div class="screen" id="flowerScreen">
    <div class="flower">🌷</div>
    <p>For you😳</p>
  </div>

  <!-- Screen 2: Envelope -->
  <div class="screen hidden" id="envelopeScreen">
    <div class="envelope">💌</div>
    <p>Open me😉😜😳</p>
  </div>

  <!-- Screen 3: Card -->
  <div class="screen hidden" id="cardScreen">
    <div class="card" id="card">
      <h1>HELLOOO CUTIE PATOOTIE😳❤️</h1>
      <p>
        I was just wondering, are you free on Valentine's Day?🤔🤔🤔<br>
        If you are, would you like to go out with me?😳😳🫣🫣
      </p>
      <p>
        Will you be my Valentine?🫣🫣🫣
      </p>

      <div class="buttons">
        <button id="yesBtn">Yes 😚💘</button>
        <button id="noBtn">No 💔</button>
      </div>
    </div>
  </div>

  <script>
    const flowerScreen = document.getElementById("flowerScreen");
    const envelopeScreen = document.getElementById("envelopeScreen");
    const cardScreen = document.getElementById("cardScreen");

    const yesBtn = document.getElementById("yesBtn");
    const noBtn = document.getElementById("noBtn");
    const card = document.getElementById("card");

    // Flower -> Envelope
    flowerScreen.addEventListener("click", () => {
      flowerScreen.classList.add("hidden");
      envelopeScreen.classList.remove("hidden");
    });

    // Envelope -> Card
    envelopeScreen.addEventListener("click", () => {
      envelopeScreen.classList.add("hidden");
      cardScreen.classList.remove("hidden");
    });

    // Yes button
    yesBtn.addEventListener("click", () => {
      card.innerHTML = `
        <h1>YAYYY🤗💕</h1>
        <p>
          I can’t wait to spend Valentine’s with you😉😜❤️.<br>
        </p>
      `;
    });

    // No button runs away
    noBtn.addEventListener("mouseover", () => {
      const x = Math.random() * (window.innerWidth - 100);
      const y = Math.random() * (window.innerHeight - 50);
      noBtn.style.left = x + "px";
      noBtn.style.top = y + "px";
    });
  </script>

</body>
</html>
